# ENGINE_REQUESTS

Requests from the iOS and server sessions to the Windows/engine session.
Append at the bottom. The engine session fills in Status when done.

| # | From | Request | Status |
|---|------|---------|--------|
| 1 | — | (example) expose `Engine::set_video_bitrate(kbps)` | open |
